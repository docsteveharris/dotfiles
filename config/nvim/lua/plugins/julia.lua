return {
  {
    -- "JuliaEditorSupport/julia-vim",
    -- ft = "julia",
    -- config = function()
    --   -- Minimal safe configuration
    --   vim.g.julia_indent_align_brackets = 0
    --   vim.g.julia_indent_align_funcargs = 0
    --   -- Disable LaTeX to Unicode to avoid errors
    --   vim.g.latex_to_unicode_auto = 0
    -- end,
  },
}
