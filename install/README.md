You'll need to update these files from time to time

```sh
brew bundle dump --mas --file=-
```


The above will print to the command line. Your choices are `mas`, `brews`, and `casks`, and if this works then do the same but adjust the file option to `Brewfile`, `Caskfile`, or `MASfile`.
